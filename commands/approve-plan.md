---
name: approve-plan
description: Human-only plan approval. The agent must not execute the approval CLI.
---

# /approve-plan

Do not run the approval script.

Tell the user to run this in their own terminal from the consumer repository:

```text
node <plugin-root>/scripts/cli/approve-plan.mjs
```
