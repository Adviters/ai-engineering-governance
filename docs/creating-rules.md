# Creating rules

Archivos `rules/*.mdc` con frontmatter `description`, `alwaysApply`, `globs` opcional.

Una concern por rule. Delegar procedimientos a skills.

`agent-governance` es la única always-on. El resto se carga por relevancia.

Rules no enforcean nada.
