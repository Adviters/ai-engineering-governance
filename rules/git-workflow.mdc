---
description: Git workflow for task branches. Use before modifying versioned files or when the current branch is protected.
alwaysApply: false
---

# Git Workflow

Use skill `start-task` before changing versioned files.

Protected branches include `main`, `master`, `develop`, and `qa` unless `.ai/config.json` overrides them.

- Inspect the current branch and working tree first.
- Reuse an existing task branch when it already matches the work.
- Create a branch only when none exists, from the current HEAD, using `git checkout -b <name>`. Never switch to `develop`/`main` to use it as the base.
- Branch name: `<type>/<ticket>-<short-summary>` when the user gives a Jira/Trello id (example: `TASK-1234 cambiar color` → `fix/task-1234-changed-color`). Lowercase the ticket. Reuse the branch if it already exists.
- Never run `git reset --hard`, `git clean`, automatic stash, or silent branch switching with dirty files.
- If the working tree makes a checkout unsafe: stop and explain.

A branch is a unit of work, not a prompt.
